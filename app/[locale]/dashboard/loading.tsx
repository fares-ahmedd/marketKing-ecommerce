const Loader = () => {
  return (
    <div className="h-[calc(100vh-122px)] w-full flex-center">
      <span className="inline-block w-12 h-12 border-4 border-dotted border-main-text rounded-full animate-rotation"></span>
    </div>
  );
};

export default Loader;
